---

---
<nav>
  <div id="navBar">
    <div id="menu">Menu</div>
    <div id="bland">P. SHIKAMUSENGE.</div>
    <div id="links" class="links">
      <a href="#home">Home</a>
      <a href="#about">About</a>
      <a href="#award">Awards</a>
      <a href="#contact">Contact Us</a>
      <a href="#portfolio">Portfolio</a>
    </div>
    <div>
      <button>
        <a
          href="mailto:shikamusenge720@gmail.com?cc=codeexp77@gmail.com&subject=You%20are%20hired"
          style="color: white; text-decoration: none;"
          target="_blank"
          rel="noopener noreferrer"
          >Hire me</a
        >
      </button>
    </div>
  </div>
</nav>
  <script>
    const menBtn = document.querySelector("#menu");

let isnavcative=false;
menBtn?.addEventListener("click",()=>{
  isnavcative=!isnavcative;
  const navlinks = document.querySelector("#links");
  console.log(navlinks);
  const deactivateNav= ()=>{
  navlinks?.classList.remove("navactive");
  navlinks?.classList.add("links");
  }
  navlinks?.querySelectorAll("a").forEach(item=>{
    item.addEventListener("click",()=>deactivateNav())
  })
  
  if(!isnavcative){
    navlinks?.classList.add("navactive");
  navlinks?.classList.remove("links");

}else
{

deactivateNav()
}
} )
  </script>
<style>
  nav {
    top: 0;
    position: sticky;
    z-index: 50;
    padding-top: 2rem;
    font-family: "Inter", sans-serif;
    font-size: 12px;
    border-bottom: 1px solid black;
    padding-bottom: 0.3rem;
    background-color: white;
  }

  #navBar {
    max-width: 1200px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-inline: auto;
    padding-inline: 2rem;
    position: relative;
    font-size: medium;
  }
#links a:hover{
  color: blue;
}
  #bland {
    font-weight: bold;
  }

  a {
    color: black;
    text-decoration: none;
  }

  button {
    padding-block: 0.3rem;
    outline: none;
    background-color: black;
    color: white;
    border-radius: 0.2rem;
    padding-inline: 1rem;
    padding-block: 0.5rem;
    cursor: pointer;
    border: none;
  }

  .links {
    display: flex;
    gap: 0.8rem;
    align-items: center;
  }
  #menu{
    cursor: pointer;
    padding: 0.5rem;
    border: solid 1px blue;
    border-radius: 3px;
    display: none;
  }
  #menu:hover{
        background-color: rgb(189, 203, 214);
  }
  @media screen and (max-width: 550px) {
    .links {
      display: none;
      flex-direction: column;
      visibility: hidden;
    }
.navactive{
  display: flex;
  position: absolute;
  background-color: white;
  display: flex;
  flex-direction: column;
  margin-top: 9rem;
  width: 100%;
  box-shadow:2px 2px rgba(164, 164, 193, 0.557);
  margin-left: -2rem;
  padding-inline: 2rem;
  padding-bottom: 1rem;
gap: 0.5rem;
}
    #menu{
      display: inline;
    }
  }
</style>
